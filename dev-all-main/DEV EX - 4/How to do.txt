step 1: Go to your project folder
mkdir flask-docker-app 

✅ Step 2: Create app.py
notepad app.py

Paste:

from flask import Flask, request, jsonify

app = Flask(__name__)

students = {}
next_id = 1

@app.route('/')
def home():
    return "Flask App Running"

@app.route('/students', methods=['GET'])
def get_students():
    return jsonify(list(students.values()))

@app.route('/students', methods=['POST'])
def add_student():
    global next_id
    data = request.get_json()
    student = {'id': next_id, 'name': data['name'], 'roll': data['roll']}
    students[next_id] = student
    next_id += 1
    return jsonify(student), 201

@app.route('/students/<int:sid>', methods=['DELETE'])
def delete_student(sid):
    students.pop(sid, None)
    return jsonify({'msg': 'Deleted'})

if __name__ == '__main__':
    app.run(debug=True)
✅ Step 3: Create requirements.txt
notepad requirements.txt

Paste:

flask
✅ Step 4: Create Dockerfile
notepad Dockerfile

Paste:

FROM python:3.10

WORKDIR /app

COPY . .

RUN pip install -r requirements.txt

CMD ["python", "app.py"]
🚀 PART 2: Fix Git & Push to GitHub
✅ Step 5: Make sure NO wrong git exists
cd C:\Users\luepi
Remove-Item -Recurse -Force .git
✅ Step 6: Go back to project
cd flask-docker-app
✅ Step 7: Initialize git correctly
git init
✅ Step 8: Add files
git add .
✅ Step 9: Commit
git commit -m "initial commit"
✅ Step 10: Connect GitHub repo

👉 First create repo on GitHub (empty repo)

Then:

git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git push -u origin main
🚀 PART 3: Add GitHub Actions CI
✅ Step 11: Create workflow folder
mkdir .github\workflows
✅ Step 12: Create workflow file
notepad .github\workflows\docker-publish.yml
✅ Step 13: Paste this YAML
name: Build and Push Docker Image

on:
  push:
    branches: [ "main" ]

jobs:
  build-push:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build Docker image
      run: docker build -t ${{ secrets.DOCKER_USERNAME }}/python-app:latest .

    - name: Push Docker image
      run: docker push ${{ secrets.DOCKER_USERNAME }}/python-app:latest
✅ Step 14: Add Docker Hub secrets

Go to GitHub repo:

👉 Settings → Secrets → Actions

Add:

DOCKER_USERNAME
DOCKER_PASSWORD (Docker access token)
✅ Step 15: Push workflow
git add .
git commit -m "Add CI pipeline"
 git push origin main
🚀 PART 4: Run & Verify
✅ Step 16: Go to GitHub

👉 Open Actions tab

✔ You will see pipeline running
✔ Green tick = success

✅ Step 17: Check Docker Hub

👉 Your image will appear:

YOUR_USERNAME/python-app:latest
